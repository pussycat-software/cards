---

version: '2'
services:
  web:
    build: .
    ports:
      - '8098:8098'
    volumes:
      - .:/code
      - ./misc/config-docker.yml:/code/config.yml
    links:
      - redis
    restart: 'always'

  redis:
    image: 'redis:alpine'
    restart: 'always'
